//Sintaxe curta de objetos -> quando o nome da propriedade do objeto for igual ao nome dado a variavel não é preciso colocar propriedade: propriedade

const nome = "Henrique";
const idade = 19;

const user = {
  nome,
  idade
}

console.log(user);
